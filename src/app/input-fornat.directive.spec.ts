import { InputFornatDirective } from './input-fornat.directive';

describe('InputFornatDirective', () => {
  it('should create an instance', () => {
    const directive = new InputFornatDirective();
    expect(directive).toBeTruthy();
  });
});
